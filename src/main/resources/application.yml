server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lxq?useUnicode=true&characterEncoding=UTF-8&zeroDateTimeBehavior=convertToNull&serverTimezone=Asia/Shanghai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        show-sql: true
        dialect: org.hibernate.spatial.dialect.mysql.MySQL56SpatialDialect
        naming:
          physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
#    database: mysql
#    hibernate:
#      ddl-auto: none

